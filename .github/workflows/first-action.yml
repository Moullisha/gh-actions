name: First Workflow
on: workflow_dispatch
jobs: 
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greetings
        run: |
          echo "Hello World!"
          echo "How are you??"
      - name: Print goodbye
        run: echo "Done -- Bye!"
  
